# Contributing

Use pnpm, keep React as a peer dependency, add tests for behavior changes, and update documentation when public APIs change.

Recommended local loop:

```bash
pnpm install
pnpm validate
```
