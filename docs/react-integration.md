# React Integration

React consumers can use the generated custom elements, but custom events are native DOM events. Use refs for property-only complex values and `addEventListener` for strongly typed event handling.
