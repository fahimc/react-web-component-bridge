# Slots

Slots are represented as React props containing `<slot>` wrappers. React does not own slotted nodes. Dynamic slot content is handled by the browser slotting model, while React renders a stable wrapper.
