# Angular Integration

Use Angular v22, `CUSTOM_ELEMENTS_SCHEMA`, and standalone components. Primitive attributes can be bound in templates. Object and array values should be assigned through `ViewChild` because HTML attributes cannot carry rich JavaScript values. Angular forms can use `ngDefaultControl` for simple controls; object-valued controls may need a custom value accessor.
