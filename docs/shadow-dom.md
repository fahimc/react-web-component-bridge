# Shadow DOM

Shadow DOM is the default. It provides style isolation, native slots, `::part()` customization, and a stable React mount part. Closed Shadow DOM is supported but reduces debugging and framework integration visibility.
