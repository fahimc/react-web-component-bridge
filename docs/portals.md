# Portals

Portal containers can live in the Shadow Root, the host, `document.body`, or a user-supplied target. Shadow-local portals preserve component styling. Body-level portals are useful for stacking and overflow but require global styles.
