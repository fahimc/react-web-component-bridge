# Vue Integration

Vue can render custom elements directly. Use refs when assigning property-only objects or calling public methods. Listen to browser custom events with Vue event syntax and read `event.detail`.
